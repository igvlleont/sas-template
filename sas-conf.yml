server:
  address: localhost
  port: 8325 # порт публикации базы на встроенном веб сервере, если одновременно несколько серверов запускается, порты должны быть разыми
database:
  # В случае файловой базы заполняем path, иначе: dbms, name, server, user, password
  # path: C:\1C_DB\sas-1 - Папка экземпляра сервера
  dbms: MSSQLServer
  name: ИмяБазыДанныхНаСервереБД
  server: АдресСервераБД
  user: ПользовательСУБД
  password: ПарольПользователяСУБД
infobase:
  id: 4d35d860-90fa-436f-b4ca-ffe32375d88d # Случайный уид
  name: sas-ib-5 # Имя базы 
  distribute-licenses: yes
  schedule-jobs: no
  disable-local-speech-to-text: no
  access-right-audit-events-recording: no
http:
  base: /
gates:
  direct:
    regport: 1545 # порт службы сервера, если одновременно несколько серверов запускается, порты должны быть разыми
debug:
  address: localhost
  port: 1555 # порт отладки, если одновременно несколько серверов запускается, порты должны быть разыми
  type: http